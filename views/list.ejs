<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, minimum-scale=1.0, initial-scale=1.0, user-scalable=yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <title>Podcasts</title>
    <link href="css/watson-bootstrap-style.css" rel="stylesheet">
    <link href="css/style.css" rel="stylesheet">

    <script src="/static/webcomponentsjs/webcomponents-lite.js"></script>
    <link rel="import" href="/static/paper-scroll-header-panel/paper-scroll-header-panel.html">
    <link rel="import" href="/static/paper-toolbar/paper-toolbar.html">
    <link rel="import" href="/static/iron-icons/iron-icons.html">
    <link rel="import" href="/static/paper-icon-button/paper-icon-button.html">
    <link rel="import" href="/static/paper-drawer-panel/paper-drawer-panel.html">
    <link rel="import" href="/static/paper-card/paper-card.html">
    <link rel="import" href="/static/paper-item/paper-item.html">
    <link rel="import" href="/static/paper-menu/paper-menu.html">

    <link rel="stylesheet" href="/static/paper-styles/demo.css">

    <style is="custom-style">
       #cards {
        max-height: 4em;
      }
      paper-card {
        @apply(--layout-vertical);
        width: 100%;
        height: 100%;
        margin-bottom: 16px;
      }
      paper-scroll-header-panel {
        position: absolute;
        top: 0;
        right: 0;
        bottom: 0;
        left: 0;
        background-color: #414B4B;
      }
      paper-toolbar {
        background-color: var(--google-red-500, #4285f4);
      }
      paper-toolbar .title {
        margin: 0 8px;
      }
      paper-scroll-header-panel .content {
        padding: 8px;
      }
    </style>
  </head>
  <body unresolved>
   <paper-drawer-panel>
    <paper-header-panel drawer>
    <paper-toolbar><div class="flex title">Menu</div></paper-toolbar>
      <div class="horizontal-section">
        <paper-menu>
          <a href="http://www.voxiter.com"><paper-item><paper-icon-button icon="home"></paper-icon-button>Home</paper-item></a>
          <a href="/"><paper-item><paper-icon-button icon="add-circle"></paper-icon-button>Add a Podcast</paper-item></a>
        </paper-menu>
      </div>
    </paper-header-panel>
     

      <paper-scroll-header-panel main>

      <paper-toolbar>
      
        <div class="flex title">Voxiter</div>

      </paper-toolbar>

      <div class="content">
        <div id="cards">
          <%if (data != null) { %>
            <% for(var i=0; i<data.length; i++) { %>
                <paper-card class='fancy'>
                    <div class="card-content">
                      <div class="title">
                        <div class="medium"><a href="/editor/<%= data[i]._id %>"><%= data[i].title %></a></div>
                      </div>
                      <div class="small"><%= data[i].post.results %></div>
                      <div class="delete"><a href='/delete/<%= data[i]._id %>'><button>Delete</button></a></div>
                    </div>
                </paper-card><br/>
            <% } %>
          <% } %>
        </div>

    </paper-scroll-header-panel>

    </paper-drawer-panel>

  </body>


</html>


